import sys

def maxProductInTriangle():
	for i in xrange(len)

print 3

if __name__ == '__main__':
	matrix = [[0 for i in xrange(15)] for i in xrange(15)]
	b = open(sys.argv[1], 'r')
	i = 0
	for line in b:
		s = line.split(' ')
		for j in xrange(len(s)):
			matrix[i][j] = int(s[j])
		i+= 1
	print maxProductInTriangle(matrix)
